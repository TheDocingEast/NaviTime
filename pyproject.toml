[project]
name = "NaviTime"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PySide6",
    "psycopg2-binary",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
NaviTime = "main:main"
